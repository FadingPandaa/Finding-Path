# Finding-Path
This Java application consists of a file system tool which uses the NIO.2 programming API. It analyzes file directories to collect data like names of files and number of elements. The analysis is done by looping through different path elements giving a clear structure in the command line interface.
